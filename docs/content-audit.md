# Content Audit — Phase 5, Chunk P5-A

Ground truth for the event pass (P5-B/C/D write against this document, not vibes). Generated by `npm run audit:events` (`scripts/eventAudit.ts`) plus hand analysis. Re-run the script after each content batch and update the "current total" section below.

## P5-D update

**Verify-before-coding findings:**
- **Generational bucket blocked as written.** Two of the four generational briefs ("new paterfamilias measured against old," "dowager's counsel") presuppose state this codebase doesn't track: no flag marks a just-completed succession (`successionEvents.ts`'s sequence sets none), and `Character.role` has no widow/dowager state distinct from an ordinary `'spouse'` — a predecessor's surviving spouse isn't distinguished from the current one. `paterfamiliasGenerations` exists as a real `GameState` counter but isn't reachable by any `EventCondition` type. Resolved with the user (Option A, of three offered): "measured against old" reframed onto a `lifetimeDignitas ≥ 300` proxy (the same kind of substitution the brief itself already sanctions for "ancestor masks"); "dowager's counsel" recast as the player's own spouse (`evt-gen-materfamilias-counsel`/`-echo`) rather than a nonexistent predecessor's widow — unconditioned, which matches the precedent P5-B already shipped for `evt-dom-sibling-friction` (no check that a second sibling actually exists either). Zero new store code for this bucket.
- **Two burn-resolution sites, not one.** `secretEngine`'s NPC AI can also choose to `'burn'` a Secret it holds against the player (a self-destructive "use it or lose it" play, resolved in `turnSequencer.ts`, not `gameStore.ts`) — and, per that code's own consequence, the *burning leader* takes the vote/reputation hit either way. Both the player-initiated (`gameStore.burnSecret`) and NPC-initiated paths therefore produce the same "a leader was just ruined by a burned Secret" fiction the aftermath brief describes, so `secret-burned-recently` is set at both sites, not just the one named in the plan's file list. Noted here since it's a small deviation from D1's literal "two tiny flag writes" (still one flag, two write-sites).
- **`endlessMode` reachability confirmed unreachable, exactly as the plan anticipated** — mirrored into `flags['endless-mode-active']` at `gameStore.enterEndlessMode`, the plan's own pre-authorized contingency.
- **Showpiece scene-count correction.** This audit's own P5-C section speculated a showpiece "counts as only ONE weight > 0 entry no matter how many scenes it spans." That was wrong — the phase plan's P5-D section explicitly says showpiece scenes chain via **flags between scenes** (Pattern D), and Pattern D delayed follow-ups are never weight 0 (guide §5.2 reserves weight 0 for `nextEventId` branch destinations only). Each showpiece scene is therefore its own random-draw-pool entry: 3 scenes × 2 showpieces = 6 pool entries, not 2. This materially changed the batch's total-count math (see below) — corrected here rather than left standing as a stale assumption.

**Pool delta:** 64 → **82** random-draw-eligible events (+18). `EVENT_DEFS` raw file count: 82 → 100. **This lands the whole random-draw pool inside the 80–120 target for the first time** (P5-D's own "Done when" criterion).

**New ids added:** Aftermath (5, flag-gated on D1's new verdict/burn flags) — `evt-aft-vindication-afterglow`, `evt-aft-prosecution-cold-shoulder`, `evt-aft-defense-lost-season`, `evt-aft-burn-collateral`, `evt-aft-burn-gossip` (the brief's two burn-adjacent beats — "the scandal's collateral" and "the Forum's gossip turns toward you" — share one flag; see the flag-race note below). Generational (4, per the Option-A resolution above) — `evt-gen-measured-against-old`, `evt-gen-materfamilias-counsel`, `evt-gen-materfamilias-echo`, `evt-gen-ancestor-masks` (the brief's "brothers' rivalry" item was dropped as substantively covered already by P5-B's `evt-dom-sibling-friction`/`evt-dom-sibling-reconciliation` arc — flagged rather than duplicated, per the established practice). Endless-mode ambience (3) — `evt-end-veterans-every-corner`, `evt-end-senate-without-enemy`, `evt-end-hothead-new-adventures`. Showpieces (2 chains, 3 scenes each) — `evt-shp-sibyl-omen` → `evt-shp-sibyl-senate` → `evt-shp-sibyl-resolution`; `evt-shp-grain-shortage` → `evt-shp-grain-syndicate` → `evt-shp-grain-reckoning`.

**Design calls made without a stop-and-ask (minor, judgment-level, logged per established practice):**
- The two burn-aftermath events (`evt-aft-burn-collateral`/`-gossip`) deliberately share the single `secret-burned-recently` flag as two independent weight > 0 consumers rather than being merged into one scene. Whichever draws first in a given burn's aftermath window clears the flag for both — a probabilistic "which flavor of aftermath you get this time" rather than a bug; over a full run with multiple burns, both get their turn across different occasions. No existing precedent for two weight > 0 defs sharing one flag was found elsewhere in the pool, but nothing in the schema or guide forbids it, and it reads as a feature (variety) rather than a defect.
- `evt-gen-ancestor-masks` hard-gates on `season.index === 3` (Winter, per the brief's explicit "a quiet Winter beat") *and* `lifetimeDignitas ≥ 350` (Tier 4, "Patrician Lord" — the brief's own "≥2 generations of Hall-worthy deeds... if nothing better" fallback).
- The Grain Fleet showpiece's syndicate-deal choice uses `createLatentSecret:provincial_plunder:2` — the guide's §6.2-sanctioned general-purpose token for a real risk/reward "this could come back to bite you" choice, matching `compromisingEvents.ts`'s existing pattern.

**D1 flag writes — final implementation:**
- `trialEngine.ts`'s `resolveTrialOutcome` (the single function every trial origin funnels through, per Phase 4 P4-E's extraction) now sets `trial-resolved-defense-won` (seat `'defense'`, outcome `acquitted`/`dismissed`), `trial-resolved-defense-lost` (seat `'defense'`, any guilty outcome), or `trial-resolved-prosecution-won` (seat `'prosecution'`, any guilty outcome) immediately before constructing the resolved `TrialState`. A losing player-filed prosecution (seat `'prosecution'`, outcome `acquitted`/`dismissed`) sets none of the three — that's the pre-existing calumnia path, which already has its own consequences.
- `gameStore.ts`'s `burnSecret` and `turnSequencer.ts`'s NPC self-destructive burn loop both set `secret-burned-recently`.
- `gameStore.ts`'s `enterEndlessMode` mirrors `endlessMode` into `flags['endless-mode-active']`.

**Verification:** `__tests__/trialEngine.test.ts` gained a dedicated `resolveTrialOutcome — P5-D aftermath flags` block (all four seat/outcome combinations). A new `__tests__/p5d.test.ts` covers `burnSecret`, the NPC-burn path via a full `processSeason` fixture built to satisfy `scanNpcSecretDecisions`'s exact eligibility filters (relationship ≤ `BALANCE.secrets.npcAi.npcBurnStandingMax`, no matching live bill, not mid-campaign), and `enterEndlessMode`. `__tests__/eventEngine.test.ts` gained a `P5-D Batch III` block: schema compliance for all 18 new ids, eligibility force-fired from fabricated matching states for every flag/resource/season gate, and explicit flag-chain assertions for both showpieces (including that Sibyl's Price's scene 2 failure branch — not just its success branch — is what escalates to scene 3). `tsc` and the full `jest` suite show no new failures beyond the same 3 pre-existing `officeAction.test.ts` failures already confirmed unrelated in P5-C (a `--experimental-vm-modules` dynamic-import issue).

## P5-C update

**Verify-before-coding finding — a real bug, fixed:** `eventEngine.ts`'s `evalCondition` for `{ type: 'office' }` read `(player as any)?.heldOffice ?? null` — a field that does not exist anywhere on `Character` (confirmed via `models/character.ts` and a full-repo grep). Every other system in the codebase (`munificenceEngine.ts`, `CursusScreen.tsx`, `ambitionEngine.ts`, `agendaEngine.ts`) reads the player's current office from `state.currentOffice` (the household-wide single-office slot), with an explicit comment in `munificenceEngine.ts` confirming the convention. The `office` condition was therefore permanently dead for the player — `held` was always `undefined ?? null`, never equal to any real office id — and P5-C's whole office-gated brief category (one of the audit's four confirmed-zero condition families) would have been unreachable content. Fixed as a one-line change (`state.currentOffice === cond.held`), confirmed with the user before writing any P5-C content, same treatment as P5-A's `COMPROMISING_EVENT_DEFS` fix. Regression-guarded in `__tests__/eventEngine.test.ts`.

**Pool delta:** 42 → **64** random-draw-eligible events (+22). `EVENT_DEFS` raw file count: 60 → 82.

**New ids added:** `evt-cri-unrest-bread-queue`, `evt-cri-unrest-demagogue`, `evt-cri-economy-moneylender-collapse`, `evt-cri-economy-contractors-abandon`, `evt-cri-constitution-veto-standoff`, `evt-cri-constitution-extralegal-command`, `evt-cri-war-widows-petition`, `evt-cri-war-refugee-families`, `evt-cri-war-profiteer-partnership`, `evt-rome-plebs-low-shop-shutters`, `evt-rome-treasury-property-levy`, `evt-rome-stability-high-complacency`, `evt-off-quaestor-account-whisper`, `evt-off-aedile-market-weights`, `evt-off-praetor-impossible-docket`, `evt-off-tribune-doorstep-supplicants`, `evt-off-consul-levy-noshows`, `evt-rep-salutatio-parasites`, `evt-rep-low-fides-social-creditor`, `evt-rep-new-man-sponsorship`, `evt-rep-new-man-vindicated` (Pattern-D follow-up), `evt-rep-new-man-embarrassment` (Pattern-D follow-up).

**Count vs. brief:** the brief's own item list (war track alone names 3 civic-face beats, not "~2") plus the "new man" brief's explicit "repayment either way" requirement (two distinct flag-gated follow-ups, one per branch) landed the batch at 22 rather than the stated ~16–20. Every entry is traceable to a named brief item, not invented padding — flagged here per this audit's own "the audit's number wins" precedent and P5-A's "aim for the top of each range" recommendation, which this exceeds slightly. Re-running after P5-D should confirm the pool is comfortably inside 80–120 without needing D to stretch its own showpiece count.

**Design calls made without a stop-and-ask (minor, judgment-level, logged per the plan's own practice rather than blocking on):**
- "High tier" (`evt-rep-salutatio-parasites`) has no dedicated condition type — Patron Tier is a pure function of `lifetimeDignitas` (`patronEngine.computePatronTier`), not stored state. Gated on `lifetimeDignitas ≥ 180`, the exact threshold for Tier 3 ("Major Patron" — "half the city eats from your hand"), the tier whose flavour text most matches a crowded salutatio.
- "Low fides" (`evt-rep-low-fides-social-creditor`) gated at `fides < 15`, roughly half the starting value (30).
- The economy-track "contractors abandon a public works site" brief's parenthetical ("aedile-flavoured if the office gate is available on top of the track gate") was read as optional tone guidance, not a strict AND — an economy-track-only gate keeps the event reachable outside the (short, intermittent) aedile term; a strict combined gate would have made it nearly unfireable, against the guide's own §5.1 principle.
- Crisis-track thresholds use tier 2 (`≥ 40`) as "elevated" per `crisisEngine.ts`'s band table, except the extra-legal-command rumor (`≥ 50`) for its more severe fiction.
- Office-gated events weighted 8 (top of the guide's post-P5-A-tightened band) since each is only ever eligible during a short, intermittent office term and would otherwise be drowned out by the ~55-strong always-eligible neutral pool.

**Net EV shift:** the audit's tracked-token sum moved from +5.0 (pre-P5-B/C) to **−246.5** post-batch, driven by denarii (sum −233.5 → −428.5, n 12 → 48). This batch adds real economy-side spending (levies, dole, tribunal patronage, sponsorship) with fewer matching income events — expected for a "Rome reacts to pressure, and reacting mostly costs something" batch, and explicitly P5-A's own prediction (¶"Income-generating content belongs more naturally to P5-C... P5-H's balance pass should treat this as a known, accepted skew"). No single event exceeds the guide's large-magnitude bands (denarii ≤ 40 per choice, crisis-track ≤ 5 per choice, fides ≤ 8 per choice). Flagged for P5-H, not corrected here per invariant 6 (`BALANCE`-only changes belong to that chunk, not a content batch).

**Verification:** `__tests__/eventEngine.test.ts` gained a `P5-C Batch II` describe block — schema compliance (weight > 0, effect strings present, only sanctioned skills, terminal-choice `successText`/`failureText` per the guide's checklist) plus eligibility tests force-firing every new event from a fabricated matching debug state (crisisTrack levels, `rome` extremes, `currentOffice`, `lifetimeDignitas`/`fides` thresholds, and both `rep-new-man` flag branches) — the pure-function equivalent of DebugPanel's force-fire tool for a chat session. `npx tsc --noEmit` and the full `npx jest` suite are clean of any new failures (3 pre-existing failures in `officeAction.test.ts`, unrelated — a `--experimental-vm-modules` dynamic-import issue predating this chunk, confirmed via `git stash` byte-for-byte match against the pre-P5-C baseline).

## P5-B update

**Verify-before-coding finding:** most of P5-B's specifically-named briefs already existed in `events.ts` before this batch (`evt-summer-heat-plague`, `evt-autumn-vintage`, `evt-autumn-harvest`, `evt-winter-election-eve`, `evt-winter-frozen-tiber` each substantively covered a brief — skipped rather than duplicated). Two existing events were weak matches and were **replaced in place** rather than duplicated (counts toward the target per the plan's own allowance): `evt-spring-census-rumor` (added the intrigus-check/denarii/flag-risk structure the brief specifically wanted, plus a new Pattern-D follow-up `evt-spring-census-scrutiny`) and `evt-winter-saturnalia` (added Philon's sanctioned cameo, per the P5-E cross-chunk voice-registers note — the existing version only said "your steward"). That left ~10-11 genuinely new brief-derived events, short of the 16–20 target and short of this audit's own "aim for the top of the range" recommendation — so the batch also includes 6 additional original seasonal/domestic events in the same domains/tone, decided with the user rather than assumed unilaterally.

**Pool delta:** 23 → **42** random-draw-eligible events (+19). `EVENT_DEFS` raw file count: 41 → 60.

**New ids added:** `evt-spring-census-scrutiny`, `evt-yr-parilia`, `evt-yr-tiber-flood`, `evt-yr-summer-opportunity`, `evt-yr-consualia`, `evt-yr-summer-street-performers`, `evt-yr-ludi-romani`, `evt-yr-autumn-magistrate-audit`, `evt-yr-winter-fireside`, `evt-dom-tutor`, `evt-dom-freedman-petition`, `evt-dom-illness-scare`, `evt-dom-sibling-friction`, `evt-dom-sibling-reconciliation`, `evt-dom-marriage-feeler`, `evt-dom-old-friend`, `evt-dom-steward-request`, `evt-dom-neighbor-dispute`, `evt-dom-family-heirloom`.

**Client-call-in aggregate brief:** skipped per this doc's own earlier finding (the §2.8 trim was never implemented — building the aggregate would be a store/engine change out of a content batch's scope).

**Net EV shift:** the audit's "net expected-value-ish sum" moved from +5.0 (pre-batch) to **−99.0** (post-batch), driven almost entirely by `denarii` (sum −70.0 → −233.5). This is a real shift, not noise — but it's the expected shape for *this specific batch's* content, not a balance bug: "seasonal and domestic texture" for a patron-class household is structurally cost-heavy (hosting festivals, helping clients and friends, tutors, floods) — spending denarii to buy fides/dignitas/plebs is the whole point of the domain, not an accident. No individual event's magnitude exceeds the guide's §6.4 bands (nothing above the existing "medium" −20 to −40 denarii tier). Income-generating content belongs more naturally to P5-C (crisis/economic-reactive) and the game's existing office/province systems — P5-H's balance pass should treat this as a known, accepted skew when re-checking the pool's overall shape after all three batches land, not something P5-B itself should have artificially corrected by inventing denarii-income events that wouldn't fit the domain.

**Verification:** smoke-tested via a throwaway test driving 16 idle seasons through `endSeason`/`resolveEvent` twice (2 game years) — zero crashes, season soft-weighting confirmed working (new events fired predominantly in their tagged season, occasionally off-season as `seasons`' ×0.4 suppression allows by design), 6 of the 19 new events surfaced within the 16-draw sample.

## Counting rule

**The 80–120 target counts the random-draw pool only** — exactly what `turnSequencer.ts` step 12 spreads into `eventEngine.pickRandomEvent`:

```
[...EVENT_DEFS, ...WAR_EVENT_DEFS, ...CADET_EVENT_DEFS, ...COMPROMISING_EVENT_DEFS]
```

filtered to `weight > 0` (weight-0 entries in those same files are follow-up/notice scenes reached only via `nextEventId` or force-injection — they can never be the random draw's result). Scripted/queued-only pools — `TUTORIAL_EVENT_DEFS`, `SUCCESSION_EVENT_DEFS`, `SECRET_EVENT_DEFS`, `CLAUDIUS_ARC_EVENT_DEFS` — never enter `pickRandomEvent` at all and are excluded from the count entirely (reported separately below for completeness).

## Current totals (as of P5-A)

| Pool | Raw entries | Random-draw-eligible (weight > 0) |
|---|---|---|
| `EVENT_DEFS` | 41 | 14 |
| `WAR_EVENT_DEFS` | 9 | 5 |
| `CADET_EVENT_DEFS` | 2 | 1 |
| `COMPROMISING_EVENT_DEFS` | 3 | 3 |
| **Random-draw pool total** | | **23** |
| `TUTORIAL_EVENT_DEFS` (scripted, excluded) | 8 | — |
| `SUCCESSION_EVENT_DEFS` (scripted, excluded) | 5 | — |
| `SECRET_EVENT_DEFS` (scripted, excluded) | 2 | — |
| `CLAUDIUS_ARC_EVENT_DEFS` (scripted, excluded) | 2 | — |

## ⚠ Finding: the random-draw pool is smaller than the plan assumed

**Current: 23. Target: 80–120. Needed: +57 to +97.**

The plan's own default assumption ("~45–55 new events total") and B/C/D's individual chunk-header targets (~16–20 + ~16–20 + ~12–16 = **44–56 total**) were evidently sized against a larger existing pool than actually exists. Taking the chunk headers' ranges at face value would land the pool at **67–79** — under the 80 floor, possibly by a meaningful margin.

**Recommendation (the audit's number, per the plan's own instruction that it wins over the default assumption):** target the *top* of each batch's stated range, not the middle, and treat **D's range as ~16–20 instead of the stated ~12–16** (D's showpieces are meant to be the highest-quality/highest-effort content anyway, and this is the natural place to make up the shortfall — no other batch is undersized relative to its own brief, D's brief just doesn't cover this much ground with 12–16 entries once you account for 2–3 of them being multi-scene showpieces, each of which counts as only ONE `weight > 0` entry no matter how many scenes it spans). That revises the total-needed math to **~48–60 new events**, landing the pool at **71–83** — still tight against 80. Recommend re-running this script after B+C land and, if the running total is trending toward the low 70s, adding a small number of extra single-scene Pattern-A flavor events to whichever domain the gap matrix below shows thinnest, rather than force-inflating D's showpiece count with weaker material.

## Gap matrix: season × domain (random-draw pool, hand-classified)

Domain classification is a judgment call (the audit script deliberately does not attempt it — id/imageKey prefixes aren't reliable domain signals). My read of the current 23:

| | Spring | Summer | Autumn | Winter | Season-neutral | **Row total** |
|---|---|---|---|---|---|---|
| **Political/Curia** | 1 (census-rumor) | 1 (road-embassy) | 1 (grain price) | 1 (election-eve) | 5 (3× client offers, 2× compromise) | **9** |
| **Family/Domus** | 1 (lustration OR planting — both plausibly family; only one counted here) | 1 (heat-plague) | 1 (vintage) | 2 (saturnalia, frozen-tiber) | 1 (cadet-visit) | **6** |
| **Military/Provincial** | 0 | 4 (campaign-fever, naval-gamble, grain-convoy-raid, legate-overreach) | 0 | 0 | 3 (weariness-murmur, carthaginian-feeler, compromise-province) | **7** |
| **Uncounted** (the second of lustration/planting, whichever wasn't counted above) | | | | | | **1** |

**Thin cells:** Autumn has only 2 events total across all domains (one political, one family) and **zero** military/provincial content in any hard-gated season except a Summer cluster. Winter has no military content (arguably correct — campaigning season is Summer per the guide's own §11 note that Summer's "civic face" excludes the actual war). Political/Curia is Spring/Summer/Autumn-light (1 each) and neutral-heavy (5) — P5-C's crisis-track/Rome-stat/office-gated/standing-reactive briefs (mostly season-neutral by nature) will keep feeding that neutral bucket; P5-B should specifically prioritize **Autumn** political/family content, since P5-B's own brief already assigns exactly 3 Autumn slots (vintage, grain-price, Ludi Romani) — the Ludi Romani slot isn't built yet, which happens to be the political one Autumn is missing.

## By condition family (random-draw pool)

`(unconditioned)`: 13 · `flag`: 6 · `campaigning`: 2 · `asset`: 1 · `governing`: 1 · `office`: 0 · `crisisTrack`/`multiCrisis`: 0 · `hasClient`/`clientCount`: 0 · `resource`/`rome`: 0

**Zero office-gated events, zero crisis-track-gated events, zero standing-reactive (`hasClient`/`rome`-stat) events exist in the random pool today.** This directly matches P5-C's brief (office-gated, crisis-track-reactive, Rome-stat-reactive, standing-reactive are exactly its four brief categories) — P5-C isn't filling a partially-covered gap, it's building these condition families from zero.

## By weight band

3–4: 4 · 5–6: 17 · 7–8: 2 · Median weight: **5**

Matches the guide's "opening scene weight 5–10" default reasonably (median sits at the low end); no outlier events above weight 8 in the current pool — batches should not exceed weight 8 without a stated reason, tightening the guide's own "10" ceiling based on actual practice.

## Effect-magnitude fingerprint (random-draw pool)

fides: sum +18.0, mean +0.95 (n=19) · denarii: sum −70.0, mean −5.83 (n=12) · plebs: sum +23.0, mean +3.29 (n=7) · lifetimeDignitas: sum +19.0, mean +3.80 (n=5) · crisis-economy/-unrest/-war: sum +3–4 each (n=1 each) · cadetStanding: sum +5.0 (n=1)

Net tracked-token sum: **+5.0** — roughly neutral already (not a real EV calc; different tokens aren't fungible, but nothing is wildly skewed). New batches should re-run this script and keep the *shape* similar — denarii trending net-negative (costs > windfalls) and fides/dignitas trending net-positive matches the existing pool's character; don't invert it without a reason.

## Other P5-A verify findings

- **The §2.8 client-call-in trim was never implemented.** `patronEngine.processFavourCallIns` (called from `turnSequencer.ts` step 18) still rolls a per-client chance every season and logs a flat summary line — it is not an `EventDef` at all, so there's no "replace the weak existing event" option here. Per P5-B's own brief: **do not build the aggregate `hasClient` omnibus event** — this is an un-implemented §2.8 trim decision outside a content batch's scope. Skip that brief entirely until the trim itself is scoped as its own small change.
- **Only two `imageKey` values resolve to a real asset**: `'portrait-paterfamilias'`, `'marius-plumber'`. Every other event (all 23 in the random pool included) already renders card-only, no portrait. Per the refreshed guide (§2.1), new events should pick natural/descriptive `imageKey` names rather than defaulting to `'portrait-paterfamilias'` — more art is planned separately.
- **Bug found, fixed in this chunk:** `gameStore.ts`'s `resolveEvent` action builds its own combined event-def lookup list and was missing `COMPROMISING_EVENT_DEFS` (it has the other 7 pools). Since `COMPROMISING_EVENT_DEFS` *is* in the random-draw pool, any of its 3 events firing via the normal random draw would silently no-op on every choice — the player taps a choice, `resolveEvent` can't find the def, and `set({ activeEvent: null })` fires with zero effect applied. This is a live, pre-existing gameplay bug (a whole Phase 4 P4-G feature partially broken), not a Phase 5 regression — fixed as a one-line addition matching `eventEngine.getEventDef`'s already-correct 8-pool list, called out explicitly in this chunk's commit.
- **`DebugPanel.tsx`'s force-fire ("FIRE EVENT" section) covered 5 of 8 real event pools** (missing `TUTORIAL_EVENT_DEFS`, `CLAUDIUS_ARC_EVENT_DEFS`, `COMPROMISING_EVENT_DEFS`) — extended to full 8-pool coverage in this chunk (A3), not built as a second mechanism.
- **The military overhaul (M1–M11), all of Phase 3, all of Phase 4, and the Mediterranean-provinces plan are built** — confirmed via `src/engine/battle/` existing with 5 files. Per the phase plan's own Cross-Chunk Notes, this changes nothing else in Phase 5's plan.
- **The 4 dead-code files the plan named for A4** (`WelcomeBackModal.tsx`/`PatronLadderPanel.tsx` duplicates, shared `DiplomatDesk.tsx`/`PolicyBoard.tsx`) no longer exist — someone already did this cleanup. A fresh whole-repo dead-code sweep (63 components/screens checked against every importer including `App.tsx`) found no additional orphans. Nothing to delete this chunk.
