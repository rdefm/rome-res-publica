# 06 — Beat III: The Ladder

**What to build:** The third and final guided beat. A teach sequence covering Cursus (offices, canvassing, elections), followed by a real ambition — "Win the Quaestorship" — with a deadline, in a 3-season sandbox with the full, uncurated event pool. This is the first beat where the player can set their own ambition alongside the tutorial's. Completing (or failing) this beat hands off into the existing embassy/war/courts chain unchanged.

**Blocked by:** 05 (sequential arc — Beat II must complete first, and player-set ambitions must already be unlocked).

**Status:** ready-for-agent

- [ ] A player entering Beat III is taught Cursus's office/canvassing/election mechanics via teach steps.
- [ ] On completing the teach portion, the player receives a real `office_held` ambition (Quaestorship) with a deadline, visible on the Ambitions tablet leaf.
- [ ] The sandbox portion runs with the full open event pool (no curation), per the world-gate plan.
- [ ] The player can additionally set their own ambition during this beat's sandbox, alongside the tutorial's goal.
- [ ] Missing the deadline resolves failed (capped Dignitas ding, one Philon narration beat) and still advances — into the existing embassy arc, unchanged.
- [ ] Save-file schema is updated for the new arc identity; a save made mid-Beat-III loads correctly.

**Implementation note (from ticket 01's review):** per the world-gate plan's per-beat table, Beat III runs `randomEvents: open` but `warIgnition: frozen` — the Carthage war still only ignites after the scripted Embassy arc, even though the rest of the event pool is fully open. `turnSequencer.ts`'s injection block only guards the forced `evt-messana-appeal` branch on `!warIgnitionFrozen` (it doesn't separately re-check `randomEventsFrozen`), so wiring `beat-ladder`'s `WORLD_GATE_POLICY` entry must explicitly keep `warIgnition` frozen — it won't default there just because `randomEvents` is open. Add a regression test (same pattern as `tutorialWorldFreeze.test.ts`'s war-ignition case) confirming Messana/Carthage still cannot ignite during Beat III's sandbox even with the full event pool live.

**Implementation note (from ticket 04's review):** assuming ticket 05 lands first as scoped in its own implementation note, `TUTORIAL_ARC_ORDER` (`tutorialEngine.ts`) will be `['beat-house', 'beat-chamber', 'prologue', 'embassy', 'war', 'courts']` and `TUTORIAL_ARCS.prologue.steps` (`tutorialScript.ts`) will start at the old Act IV (Provinciae). This ticket needs to:
- Insert `'beat-ladder'` into `TUTORIAL_ARC_ORDER` between `'beat-chamber'` and `'prologue'` (and into `TutorialArcId`/`saveLoad.ts`'s `activeArc` enum, same pattern as `'beat-house'`/`'beat-chamber'`).
- Remove `PROLOGUE_ACT5_STEPS` from `TUTORIAL_ARCS.prologue.steps` once its content moves into `beat-ladder`, leaving `prologue` holding only the old Act IV (Provinciae asset purchase). Worth flagging to the design lead once you're here: Act IV's own scripted "buy an asset in Campania" content is already functionally duplicated by ticket 02's `lesson-provinciae`/`lesson-assets` just-in-time lessons — at that point `prologue` is a single-act arc whose only job overlaps a lesson that already exists. Not this ticket's call to resolve unilaterally (CLAUDE.md's "no assumptions on ambiguous game-design questions" applies squarely here), but real and worth raising rather than silently carrying `prologue` forward as a one-act arc.
- Update `data/offices.ts`'s tutorial-only guaranteed-success branch on `Audit a Rival` (around line 134): `state.tutorial?.activeArc === 'prologue' && state.tutorial?.stepId === 'prologue.act5.audit' && leaderId === CLAUDIUS_LEADER_ID`. This checks the OLD Act V's arc/step ids directly (not a generic "any tutorial audit step" check) — once Act V's audit step moves into `beat-ladder` under new ids, this condition silently stops matching and the scripted Claudius-audit-guaranteed-success teaching moment quietly starts rolling for real instead (no retry taught, so an unlucky miss could softlock a new player). `tsc`/tests won't catch a stale string literal like this — must be updated by hand to whatever the new arc/step id ends up being.
- `philonAdvisoryUnlocked` is already `true` by the time `beat-ladder` starts (moved to end of Beat II by ticket 05), so this ticket doesn't need to touch that flag — but per this ticket's own checklist ("player can additionally set their own ambition... alongside the tutorial's goal"), double-check the tutorial's `office_held` ambition and a player-set one can coexist without colliding: `ActiveAmbition`'s scope slots are `family`/`character` (`models/ambition.ts`) and `evictOccupantForDirectWrite`/`findActiveAmbitionInSlot` (`ambitionEngine.ts`) key on `(scope, assignedCharacterId)` — so the tutorial ambition and a player-set one only avoid colliding if they're placed in different scope slots (or the tutorial ambition's own slot is deliberately left for the player). Worth confirming which scope `office_held` naturally occupies before wiring this up.
